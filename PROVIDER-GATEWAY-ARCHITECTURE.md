# 供应商网关架构（PROVIDER-GATEWAY-ARCHITECTURE）

> **本文件是「供应商网关（原智能路由）」领域的唯一事实源（SSOT）**，2026-09-16 立。
> 它回答的问题是：**这个域到底是什么、由哪些层构成、两类供应商为何不对称、有进程侧的切换与资源如何被设计。**
>
> **立此文件的背景（必读）**：本域**从未被真正设计过**——13 个文件在同一初始提交（`6a40b1d`，
> 整个内核一次性导入）中随仓搬入，此后仅有零星修补（`switch.js` 全程 1 次提交）。
> 因此`RouterService` 累积了 **69 个方法**（26 个下划线内部），职责重叠、抽象靠运行时猜测。
> 本文不是"给既存代码补文档"，而是**第一次给出该域的设计**。

---

## §1 正名与定位（★ 最重要的设计决策）

### 1.1 名字与实质的背离

代码里它叫 **Router（路由）**，但实测其语义**恰恰相反**：

| 证据 | 出处 |
|---|---|
| "**绝不跨供应商 failover**——每个供应商独立端点只在自己的账号池内选号" | `switch.js:8` |
| "**无公用/默认入口**：每个已激活供应商持有自己独立的 API 端点（端口/账号池隔离）" | `index.js:4` |

**即：它不做路由（无统一入口、不跨池分发），它只为每个供应商各起一个端口 + 各管一池账号。**

### 1.2 设计决策 **A1：明确不做跨供应商路由**

这是**显式声明的设计决策**，不是"尚未实现"：

- 每个供应商持有**独立端点**（独立端口 + 独立账号池）；
- **无统一入口**——不存在"一个端口按规则分发到多个供应商"；
- **绝不跨供应商 failover**——A 供应商无可用账号时**不**回落到 B 供应商。

**为什么这样设计（而非统一路由）**：
1. **隔离优先**：不同供应商的凭证/配额/风控策略互不可见，跨池分发会把故障与配额耦合起来；
2. **语义诚实**：调用方连的是"某个供应商"，不是"某种能力"——统一入口会掩盖这一点；
3. **计费清晰**：账号级用量/费用统计必须绑定到具体供应商。

> ⚠️ **本域的正确名称是「供应商网关（Provider Gateway）」**。文档与新增代码应使用该名；
> 既有 `RouterService` 等标识符的改名属 Phase 1（无行为变更），见 §7。

### 1.3 设计决策 **A2：隔离作为设计原则固定**

| 隔离维度 | 规定 |
|---|---|
| **端口** | 每供应商端点独占端口（providerApi 池）；反代实例独占端口（managed 池）|
| **账号池** | 完全隔离；选号只在**本供应商池内**进行 |
| **进程** | 有进程侧：每账号一个独立进程（见 §4）|
| **凭证** | key **只经环境变量**注入，**绝不进命令行**（可被 `ps` 读取）|

---

## §2 两类供应商的本质不对称（设计的出发点）

### 2.1 对照表

| 维度 | 无进程 `key-pool` | **有进程 `process-pool`** |
|---|---|---|
| 账号的资源载体 | 无（裸 key） | **一个独立进程 + 一个端口** |
| 切换成本 | **≈0**（换 key 即发请求） | **spawn + 探活**（实测最坏 **27s**，见 §4.3）|
| 无感条件 | **天然无感** | **必须目标实例已就绪（HOT）** |
| 资源约束 | 无 | **进程数上限**（内存/CPU/端口）|
| 失败域 | 单请求 | **进程级**（该账号所有请求同生共死）|
| 状态机 | 账号**单层** | 账号 + 实例 **双层** |
| 隔离要求 | 无 | 进程 / 账号 / 端口 **三重隔离** |

### 2.2 ★ 有进程侧的唯一核心矛盾

```
无感切换   ←要求→   热备（提前拉起进程）
资源节制   ←要求→   冷启（不能全量拉起）
```

**现状解法与缺陷**（实测）：

| 现状 | 缺陷 |
|---|---|
| 常驻 1 + 备胎 1（`_needSpare` 阈值 **80%** 额度） | ① resident 突遭 banned/网络错 → **无备胎** → 等 27s |
| 按需拉起 `_waitHealthy`（6 轮 × 3s + 1.5s 间隔） | ② **27s 的同步等待本身就是"有感"** |
| **预热机制已被整体废除** | ③ 因旧实现死循环而**因噎废食** → 连"提前准备"都没了 |

**结论：现状不是"没做好"，而是这个矛盾从未被正面设计过。** §4 正面解它。

---

## §3 共享层设计（两类供应商共有）

### 3.1 四层职责

```
ProviderGateway（编排）
   ├── EndpointMgr    端点与运行载体（无进程侧为空实现）
   ├── AccountPool    账号池：状态机 + 选号 + 配额
   ├── Forwarder      转发：整形 → 上游 → 透传 → 记账
   └── Provider       模式契约（key-pool / process-pool）
```

| 层 | 职责 | 唯一性要求 |
|---|---|---|
| **EndpointMgr** | 为供应商建立/监控/拆除运行载体 | 端口分配**单一实现**（现状有两份，见 §6）|
| **AccountPool** | 池内账号完整生命周期与可用性 | 状态机与 `isAccountUsable`/`addAccount` **单一实现**（现状 base/proxy 各一份）|
| **Forwarder** | 一次请求的运输与记账 | 只依赖**显式接口**，禁止 `typeof` 猜测 |
| **Provider** | 两类 pattern 的差异 | 能力以 **capability 显式声明** |

### 3.2 账号状态机（收口）

**设计原则：状态词表唯一声明；无写入方的状态必须删除。**

| 状态 | 写入方 | 处置 |
|---|---|---|
| `ready` / `frozen` / `banned` / `discarded` / `registering` | 有（`_setStatus`）| 保留 |
| `review` | **无**（`confirmAccount` 判断但无人写）| **删除**（含 API 端点）|
| `limited` | **无**（已收敛为 `frozen`）| **删除**读取分支 |

详见 §6 问题映射（P2-2/P2-3）。

### 3.3 写权唯一性（决策 **A3 的推论**）

```
设计：任一时刻，一个域的持久化文件只能有【一个写入者】。
  providers.json           ← daemon 独占（守卫内嵌实例彻底只读）
  router-usage-totals.json  ← daemon 独占（★ 当前不受写闸约束，见 P1-3）
  ports-router.json         ← daemon 独占
```

**现状缺陷**：`_save()` 受 `_persistEnabled` 约束，但 `_writeTotals()` **不受** → "守卫只读"仅成立于 providers.json。

**设计**：**统一写权闸**——所有落盘经同一入口校验写权，而非每处自行判断。

---

## §4 有进程侧深度设计（★ 本方案重点）

### 4.1 实例三态模型（决策 **B1**）

```
❌ 现状：registered/starting/running/unhealthy/stopped/failed（6 态，语义重叠，且 freeze/unfreeze 未接线）
✅ 设计：按【服务能力】分 —— 这是切换策略唯一需要的语义

  COLD   未启动（无进程）                     资源占用 0
  WARM   启动中（有进程，未就绪）              资源占用 1，不可服务
  HOT    就绪（有进程，探活通过）              资源占用 1，可立即服务
  DEAD   异常（进程在但不健康）                资源占用 1，待回收
```

**收益**：切换策略只需问"目标是否 **HOT**"，不再在 6 个状态间推导。

### 4.2 热备池 WarmPool（决策 **B3**）

```
池容量（配置化；取代硬编码"1+1"）：
  maxHot   同时 HOT 的进程上限（默认 2）
  maxWarm  同时 WARM 的进程上限（默认 1）
  → 总进程上限 = maxHot + maxWarm   ← “资源节制”的硬闸

成员选择优先级：
  ① resident（当前在用）
  ② 备胎：可用 ∧ 额度占用最低
  ③ 【新增】故障接管候选：resident 近期失败时的次优账号

目标态：desiredHot = 1 + (需要无感切换 ? 1 : 0)
```

### 4.3 切换策略：双预算模型（决策 **B2/B7**）—— **解 27s 硬伤**

```
❌ 现状：目标未 HOT → 同步等 _waitHealthy（最坏 27s）→ 用户干等

✅ 设计：双预算
  ① 请求路径同步预算（switchBudgetMs，默认 2000ms）
       目标 HOT       → 直接转发（0 等待）
       目标 WARM/COLD → 最多等 switchBudgetMs
                        超预算 → **立即换号**（快速失败，不长等）
  ② 后台异步预置（不阻塞当前请求）
       换号的同时，把该账号异步拉向 HOT
       → 下次请求到达时它已就绪 = **真正的无感**

★ 把"同步等待"改为"异步预置 + 快速失败"
  用户感知从最坏 27s 降到 ≤2s，且后续请求无感。
```

### 4.4 预热的规范化（决策 **B4/B5**）—— 恢复但设计化

```
现状"预热被废除"是【因噎废食】：旧实现死循环的根因是
  「触发条件」与「资源上限」都没有设计（只按 percent≥80，且与回收是两条相向路径）。

✅ 正确设计：预热 = Reconciler 统一决策的"让候选池成员趋近 HOT"

触发条件（明确化，取代单一 80% 阈值）：
  ① resident 额度 ≥ 80%                     （现状保留）
  ② resident 近 N 次请求有失败               （新增：故障前兆）
  ③ resident 配额窗口剩余 < 切换所需时间      （新增：时间维度）
  ④ 池内 HOT 数 < maxHot 且存在可用候选       （新增：资源允许）

资源约束：受 maxHot/maxWarm 硬闸
幂等性  ：Reconciler 单飞
★ 关键：预热与回收是【同一个决策者】（Reconciler），
  不再有"两条相向路径"——那正是旧死循环的根因。
```

### 4.5 隔离与资源治理

| 维度 | 规定 |
|---|---|
| 进程隔离 | 每账号一独立进程（保留现状，正确）|
| 账号隔离 | key **只经 env**，禁止 cmdline（写入门禁，见 §6 门禁 PG-6）|
| 端口隔离 | `claimSlot` 持久绑定（保留）|
| **资源上限** | `maxHot`/`maxWarm` 显式闸（新增）|
| 闲置回收 | 90s 宽限（保留，参数化）|
| **状态单一源** | 统一 `instanceOf(acc)`；`acc.instance` 内联引用**废除**（现状已部分收敛）|

### 4.6 决策 **A4**：网关自身的运行条件

```
❌ 现状：config.routerAutostart（"用户开关"）—— 与"基础设施无用户开关"矛盾，
        且与生命周期 desired 轴并存（两轴），契约 §2/§3 G-3 互相打架。

✅ 设计：删除 routerAutostart，改为派生条件——
        needsGateway = providers.some(p => p.activated)
        即：只要存在任一已启用供应商，网关自身就应当运行。
        网关自身的运行状态【不对用户暴露】（域 B）。
```

**域归属（决策 A7，与 GUARD-DOMAIN-MODEL 对齐）**：

| 对象 | 域 | 用户开关 |
|---|---|---|
| **供应商启用/停用**（`activated`）| **域 A**（用户意图的产物）| ✅ 有（面板操作供应商）|
| **网关自身运行** | **域 B**（基础设施）| ❌ 无（由 `needsGateway` 派生）|
| **daemon 进程** | 域 B（实现细节）| ❌ 无 |

---

## §5 契约与 capability（决策 **B6**）

### 5.1 两类 pattern 的显式声明

```
pattern: 'key-pool'（无进程）
  capabilities: { accountPool, quotaDetect?, forward }
  switch:       同步换号（0 成本）
  state:        账号单层

pattern: 'process-pool'（有进程）
  capabilities: { accountPool, quotaDetect, instanceLifecycle,
                  warmPool, switchBudget, reconcile }
  switch:       双预算（同步 ≤ switchBudgetMs + 异步预置）
  state:        账号层 + 实例层（COLD/WARM/HOT/DEAD）
  resource:     maxHot / maxWarm
```

### 5.2 契约的两个硬要求

| 要求 | 理由 |
|---|---|
| **抽象方法必须显式声明**，构造期校验，缺失即失败 | 现状：基类只声明 `detectAccount` 1 个；Forwarder 依赖 proxy 的 **13 个未声明方法**，全靠 `typeof` 猜测 |
| **不支持的能力必须显式拒绝**，不得静默降级 | 现状：direct 对 `official-billing` 配额面**静默返回 null** |

### 5.3 ctl 白名单（安全面）

```
❌ 现状：ctl.js 反射调用 router[method].apply —— 26 个下划线内部方法全部可达
        （含 _save / _stopAll），仅靠 127.0.0.1 保护。

✅ 设计：显式白名单——各层声明自己的可远程调用面；
        下划线内部方法默认不可达。（门禁见 §6 PG-5）
```

---

## §6 问题映射与门禁

### 6.1 审计问题 → 根治手段（非补丁）

| 审计问题 | 根治手段 |
|---|---|
| P0-1 前端 busy 失效 | 随 §3.1 Provider 层契约统一 |
| P0-2 ctl 无白名单 | §5.3 显式白名单 |
| P0-3 daemon argv 不符声明 | 入口规范：**只认 env 或只认 argv，二选一并写死** |
| P1-1 proxy.js 职责过载（1006 行）| §3.1 EndpointMgr 分离 |
| P1-2 隐式契约 | §5.2 构造期校验 |
| P1-3 写权不受闸 | §3.3 统一写权闸 |
| P1-4 域模型矛盾 | §4.6 派生条件 + 域归属表 |
| P1-5 域间双向依赖 | §7 Phase 1（ports/probe 上游化 platform）|
| P1-6 四组重复实现 | §3.1 每层单一实现 |
| P2-2/3 review/limited | §3.2 无写入方即删除 |
| P2-4 evidence 未接线 | 决策 **A5：删除**（无消费方、默认关）|
| P2-1/6/7/8/10/11/12 死代码 | 随各层重建自然消失 |
| P3-1..6 注释债 | 各层契约重写时一并更正 |

### 6.2 门禁（把设计变成可执行断言）

| 门禁 | 断言 |
|---|---|
| **PG-1** | 两类 pattern 的 capability 声明与实际方法一致（抽象方法缺失 → 失败）|
| **PG-2** | 转发层不出现 `typeof xxx === 'function'` 的能力猜测（应为 `supports()`）|
| **PG-3** | 实例态只用 COLD/WARM/HOT/DEAD（旧 6 态词表不得出现在新增代码）|
| **PG-4** | 资源上限：`maxHot`/`maxWarm` 存在且被 Reconciler 实际引用 |
| **PG-5** | ctl 只能调用白名单内方法（内部方法不可达）|
| **PG-6** | 凭证只经 env 注入，**不得出现在 spawn 命令行** |
| **PG-7** | 写权：所有落盘函数都经统一写权闸（无绕过）|
| **PG-8** | 反向：判据能识别旧形态（门禁非空转）|

---

## §7 实施阶段（不一次性重写）

```
Phase 0  设计冻结（本文）                      ← 当前
Phase 1  正名 + 层边界（无行为变更）
           · ports/probe 上游化到 platform/    （先做，解 P1-5，是后续前提）
           · 四层文件边界建立（骨架，旧代码暂留）
Phase 2  契约显式化
           · PROVIDER-CONTRACT.md + 构造期校验
           · ctl 白名单
Phase 3  逐层迁移（可独立验证）
           · 账号状态机合一（风险最低，先做）
           · EndpointMgr 分离（解 P1-1）
           · Forwarder 接口收口
Phase 4  有进程侧深化（§4）
           · 实例三态 + WarmPool + 双预算切换
Phase 5  收口
           · 死代码/未接线清理；门禁全覆盖
```

---

## §8 决策记录（本设计的依据）

| # | 决策 | 状态 |
|---|---|---|
| A1 | **不做跨供应商路由**（无统一入口）| ✅ 已确认 |
| A2 | 端口/账号池隔离作为设计原则固定 | ✅ 已确认 |
| A3 | 删除 `routerAutostart`，改派生条件 `needsGateway` | ✅ 已确认 |
| A4 | 网关自身运行不对用户暴露（域 B）| ✅ 已确认 |
| A5 | `evidence` 子系统**删除** | ✅ 已确认 |
| A6 | `review` 状态**删除** | ✅ 已确认 |
| A7 | 域归属：供应商启用=域 A；网关/daemon=域 B | ✅ 已确认 |
| B1 | 实例三态 COLD/WARM/HOT/DEAD | ✅ 已确认 |
| B2 | 双预算切换（同步 + 异步预置）| ✅ 已确认 |
| B3 | `maxHot`/`maxWarm` 配置化（默认 2/1）| ✅ 已确认 |
| B4 | 预热**恢复并规范化**为 Reconciler 统一决策 | ✅ 已确认 |
| B5 | 预热触发条件扩展（①额度 ②失败 ③时间 ④资源）| ✅ 已确认 |
| B6 | 两类 pattern 在契约中显式区分 | ✅ 已确认 |
| B7 | `switchBudgetMs` 默认 **2000ms** | ✅ 已确认 |

# 事故复盘：凭据被覆盖（2026-09-13）

> 等级：**P0（凭据不可逆丢失）** · 影响：REST API 能力中断 · 恢复：重新签发令牌
> 责任人：执行 agent（本人操作失误）· 关联：`CREDENTIALS-STANDARD.md` 第 4 条铁律

---

## 1. 时间线（含可核证据）

| 时刻 | 事件 | 证据 |
|---|---|---|
| 21:44 | 建立规范凭据库；`kernel-advgyxqamf.pat` = **93B** 真令牌；旧路径改为**符号链接** | `ls -la` 显示 93B |
| 21:52 | 写出 `cred.sh`（含 `put` 子命令；**当时无真机保护**）| — |
| 22:32 | 改门禁为宿主无关（D 组夹具 + R 组真机审计）| — |
| 22:57 | 跑注入验证：**注入 2 = 移除 `DSH_CRED_DIR`** 以破坏夹具模式 | 注入脚本输出「已注入」|
| 22:57 | 同一门禁内的 **D-6 `put`** 步骤因此**回落真机库根**执行 | 文件被改写为 16B |
| 22:57 | `kernel-advgyxqamf.pat` 变为 **16B**（内容 `new-secret-value`）| `stat -c %s` = 16 |
| 23:05 | 推送后**所有 API 返回 401**，据此发现事故 | `Bad credentials` |
| 23:1x | 抢救核实：备份包（09-10）早于令牌创建（09-11 10:55）；包内只有另一枚已失效令牌 | `tar tzf` + 大小/尾号 |
| 次日 | 用户重新签发两枚令牌；入库并 `verify` = HTTP 200 | `cred.sh verify` |

### 关键量化事实

```
真令牌：93 字节（github_pat_…）
覆盖后：16 字节（new-secret-value）
可恢复副本：0（唯一副本 + 符号链接 = 零冗余）
发布链路影响：0（push 走 SSH、Release 走 CI GITHUB_TOKEN、npm 走 NPM_TOKEN）
REST API 影响：全部（查 CI / 合并 PR / 改分支保护）
```

---

## 2. 直接原因链（为什么是它）

```
① 我给 cred.sh 设计了 put（破坏性写操作）
        ↓
② 我用 DSH_CRED_DIR 做「夹具库」与「真机库」的隔离（默认值 = 真机库）
        ↓
③ 我的注入验证**故意破坏了这个隔离**（移除 DSH_CRED_DIR）
        ↓
④ 同一次门禁运行里还有 put 步骤 → 落到真机库
        ↓
⑤ 旧路径是符号链接 → 覆盖立即生效，且没有第二份副本
        ↓
⑥ 结果：真令牌不可逆丢失
```

**任何一环不同，事故都不会发生。** 但每一环单独看，我当时都觉得「没问题」——
这正是它值得复盘的原因。

---

## 3. 根因（四层，从浅到深）

### 3.1 直接：破坏性操作没有默认保护

`put` 是**不可逆**的覆盖写，但它的默认目标是**真机库**，且**无确认**。
正确设计是「白名单式确认」：默认拒绝真机，测试才放开。

### 3.2 结构性：隔离是「软」的，且失效方向错误

我用 `DSH_CRED_DIR` 做隔离，默认值指向真机。于是**隔离失效的后果是降级到真机**。
更安全的形态是：**默认指向隔离区，指向真机需要显式确认**（方向相反）。
我在同一节里反复写「隔离失效必须失败」，却把破坏性命令放在了**失效即降级**的路径上。

### 3.3 方法性：注入验证选了**破坏性注入点**

注入的目的是「让门禁失效以证明它会失败」。我选了「破坏夹具隔离」——
而这条路径**同时**是执行真机写的路径。
若改注入「让 `put` 的确认逻辑失效」，门禁会失败，且**完全不会碰真机**。
**注入点的选择本身就是安全决策**，我之前没把它当回事。

### 3.4 冗余性：迁移制造了「单一副本 + 符号链接」

我为了「向后兼容」，把旧路径改成了指向新位置的符号链接。
这看起来优雅（无重复明文），实际上**删除了唯一的恢复手段**：
如果当时保留一份（哪怕是临时备份），这次事故就是可逆的。
**轮换/迁移期的第一原则是「先保证可回滚」，不是「先保证优雅」。**

---

## 4. 为什么既有防线没拦住

| 既有防线 | 为何失效 |
|---|---|
门禁（101 文件 / 1984 断言）| 门禁**不含**「破坏性操作必须对真机默认拒绝」这一条 —— 这个不变量当时不存在 |
`doctor` 卫生检查 | 它检查权限/散落副本/缺项，**不检查「写入是否被允许」** |
cite「剥离注释」「反向断言」等纪律 | 都是**断言正确性**的纪律，不覆盖**操作安全性** |
出事后我自己的第一反应 | 以为是令牌过期 —— 说明「凭据被覆盖」此刻**不在心智模型里** |

> 教训：**正确性纪律不等于安全性纪律。** 前者问「判据对不对」，后者问「操作会不会造成不可逆后果」。
> 本仓此前只有前者。

---

## 5. 已实施的修复与验证

| 加固 | 实现 | 验证 |
|---|---|---|
真机默认拒绝 | `put` 需 `DSH_CRED_ALLOW_OVERWRITE=1`；目标已存在再需 `DSH_CRED_FORCE=1` | W-1/W-2 |
覆盖前备份 | `<file>.bak-<时间戳>`（0600）| W-3（断言备份内容 = 原值）|
失效即失败 | 隔离被破坏时**拒绝**并解释两种正确用法 | W-1 |
仓库内无未隔离 `put` | 静态扫描（跳过注释）| W-5 |
门禁**不再依赖真机状态** | fakeReal 模拟法：复制 `cred.sh`、改写真机常量为临时路径 | W-1..W-4 在任意宿主确定性运行 |

### 事故场景重放（决定性）

```
重现当时的破坏（把 STORE 改回硬编码真机库）后执行 put：
  put 退出码 = 2
  ✅ 真机令牌未被改动（事故已不可能重演）
```

---

## 6. 残余风险（诚实列出）

| 风险 | 现状 |
|---|---|
令牌曾在对话中以明文出现 | 已入库（0600）；若该对话记录被留存，建议按需轮换 |
真机库无异地备份 | 单点。建议在 `put` 成功后另存一份离线/密码管理器副本（新流程待定）|
`cred.sh` 的其它子命令 | `list`/`get`/`path`/`verify`/`doctor` 只读，无破坏性；改库的写路径只有 `put`（已加固：空 stdin 先拒再动盘、真机值须 `DSH_CRED_FORCE=1` 且先尽力 `.bak`）。`backup` 不改库，但会把**整份明文令牌**复制到调用者指定目录，故带「目标落在实例子目录即拒绝」的闸 |
其它脚本的破坏性操作 | 现役脚本里唯一不可逆的是 `publish-core.sh --publish`（npm 同版本不可重发；须 `GITHUB_ACTIONS=true` 才放行）；`bump.sh --core` 的写入面 = `package.json.version`（唯一事实源）+ `package-lock.json` 的两处 `version`（跟随，门禁 P-9 B26 要求两者一致）。两者的**放行条件**由 `release-auth-test`（R7-b）与 `all-platforms-test`（T2-b / T2-b2）断言，但写路径本身未过本事故同类的「读失败不得覆盖」加固。条目里曾点名的 `release-core.sh` **已删除**，不再是风险面 |

---

## 7. 流程变更（写进规范）

1. **破坏性操作清单化**：任何不可逆操作（覆盖凭据 / 发 npm 包 / force push / 删分支）
   必须先声明「会不会不可逆 → 有无备份 → 失效方向是否安全」；
2. **注入验证前评估注入点**：优先选**非破坏性**注入点；必须选破坏性时，先确认该路径**不可能触达真机**；
3. **迁移/轮换保留回滚**：新值验证通过前，旧值必须有独立副本；
4. **正确性纪律 + 安全性纪律并重**：门禁审「判据对不对」，安全审「操作是否可逆」；
5. **「显式 SKIP」也可能是缺口**：本次门禁首版在真机无凭据时 SKIP 了 W-1..W-4 ——
   那等于保护**从未被验证**。已改为 fakeReal 模拟，确保断言始终真的在跑。

---

## 8. 一句话总结

> 我用「破坏隔离」的方式去**证明**一道门禁有效，
> 而那道隔离保护的正是**不可逆操作** —— 结果证明，需要被证明的其实是**隔离本身**。

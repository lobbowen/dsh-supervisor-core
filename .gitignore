# 截图取证
screenshots/

# build 产物（shell 壳 + 内核 SEA + npm 子包组装，全部可重新生成，绝不入库）
src-tauri/target
src-tauri/gen/schemas
src-tauri/frontend
dist/release
dist/sea
dist/npm
dist/export-shell
# React UI 构建产物（release.sh 从 ui/dist 构建后镜像，不入库）
ui-react/

# 测试残留/临时目录
tmp-iso/

# 依赖
node_modules/

# 系统/编辑器
.DS_Store
*.log

# 产品源码仓不含文档/审计/过程物（本地保留，不入库）
DESIGN.md
CODE_REVIEW.md
MIGRATION.md
docs/

# 内部操作手册（本地保留，不入库）
publish-and-verify.md
verify-desktop.md
